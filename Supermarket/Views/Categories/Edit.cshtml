@model Category
@{
    ViewData["Title"] = $"Edit Category: {Model.Name}"; // This is the text in the tab bar
}

@section Title
{
    @if (Model != null)
    {
        <h3>Category: @Model.Name</h3>
    }
}

<partial name="_Category" model="@Model" />